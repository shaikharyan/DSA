#include <iostream>
using namespace std;
int main()
{
    // Arithmetic Operator
    /* int x=10,y=3;
    int x, y;
    cin >> x >> y;
    cout << "addition:" << x + y << endl;
    cout << "Substraction:" << x - y << endl;
    cout << "Multiplication:" << x * y << endl;
    cout << "Division:" << x / y << endl;
    cout << "Modulo:" << x % y << endl;*/

    /*post-increment and pre-increment
    int x = 10;
    int y;
    y = ++x;
    cout << x << endl << y << endl;
    y = x++;
    cout << x << endl << y << endl;*/

    /*post-decrement and pre-decrement

    int x = 10;
    int y;
    y = --x;
    cout << x << y << endl;
    y = x--;
    cout << x << y << endl;*/

    /*Relational operator   1=true and 0=false

    int x = 6, y = 3;
    cout << "x<y:" << (x < y) << endl;
    cout << "x<y:" << ((x + 1) < (y - 5)) << endl;
    cout << "x>y:" << (x > y) << endl;
    cout << "x<=y:" << (x <= y) << endl;
    cout << "x>=y:" << (x >= y) << endl;
    cout << "x==y:" << (x == y) << endl;
    cout << "x!=y:" << (x != y) << endl;

    x += 3;
    cout << x << endl; // 6=3=9
    y -= 2;
    cout << y << endl; // 3-2=1   */

    /*Logical operator

    int x = 5, y = 10;
    if ((x > 5) && (y < 15))
    {
        cout << "x+y:" << (x + y) << endl;
    }
    else if ((x <= 5) || (y > 15))
    {
        cout << "x-y:" << (x - y) << endl;
    }
    else
    {
        cout << "!x:" << !x << endl;
    }*/

    /*Bitwise Operator

    int x = 26, y = 9;
    cout << "x&y:" << (x & y) << endl;
    cout << "x|y:" << (x | y) << endl;
    cout << "x^y:" << (x ^ y) << endl;
    cout << "x<<1:" << (x << 1) << endl;
    cout << "x>>1:" << (x >> 1) << endl;
    cout << "~x:" << (~x) << endl;*/

    /*Cast operator and sizeof operator

    float x = 6.789;
    cout << sizeof(x) << endl;
    cout << (int)x << endl;
    cout << x << endl;*/

    /*conditional Operator

    int a = 3,b = 12;
    cout << "Maximum is:" << ((a > b) ? a : b) << endl;
    cout << "Maximum is:" << ((a > b) ? (a + 2) : b) << endl;
    cout << "Maximum is:" << ((a > b) ? (a + 2) : (b - 5)) << endl;*/

    return 0;
}